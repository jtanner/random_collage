h1. Random Collage

h2. Installing RMagick

  sudo port install tiff -macosx
  sudo port install ghostscript
  sudo port install ImageMagick
  sudo gem install rmagick

h2. Other gem dependencies

* trollop
* active_support